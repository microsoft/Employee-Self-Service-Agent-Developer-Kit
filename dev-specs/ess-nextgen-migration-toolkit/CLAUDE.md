# ess-nextgen-migration-toolkit (specifications) — Claude Code Context

This folder is the **specification source of truth** for the ESS NextGen
Migration Toolkit (Specification-Driven Development).

Read [`AGENTS.md`](AGENTS.md) in this folder for the full AI Agent Operating
Manual — specification classification, the dependency-based loading model,
specification hierarchy, repository invariants, and the implementation
algorithm. It is the canonical constitution; this file adds no independent
rules.

The buildable toolkit (code) lives separately under
`../../tools/ess-nextgen-migration-toolkit/`.
