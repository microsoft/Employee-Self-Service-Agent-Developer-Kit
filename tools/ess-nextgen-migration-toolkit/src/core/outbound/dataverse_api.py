"""Dataverse REST wrapper.

Owns all Dataverse communication (authentication, REST helpers, serialization,
retry handling). Implemented by TASK-004. This module is intentionally a
placeholder created by the repository scaffold (TASK-001); business logic must
never live here.
"""
