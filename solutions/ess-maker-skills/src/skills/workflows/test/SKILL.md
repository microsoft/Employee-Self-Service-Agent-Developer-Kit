# Test Workflow Skill

This skill is not yet implemented. End-to-end workflow testing is currently a manual step in Power Automate.

## What to do today

Tell the user:

> Workflow testing isn't automated yet. Open the workflow in Power Automate (make.powerautomate.com -> your environment -> Solutions -> the solution that contains the workflow), open the workflow, and click **Test** -> **Manually**. Provide the inputs the workflow expects. The run history shows each action's input/output - that's where you'll see auth failures or schema mismatches.

Then stop. Do NOT pretend to run a test or report results.

<!-- TODO: Replace this stub with a real automated flow once the workflow runner exists. -->
<!-- TODO: Common workflow failures, connection auth issues, input/output debugging -->
