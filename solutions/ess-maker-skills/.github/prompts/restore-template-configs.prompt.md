---
mode: agent
description: "Type Enter to check template-config restore availability"
---

# Restore Template Configs

Tell the user:

> Template-config restore is no longer supported. It belonged to the retired
> Dataverse-based agent setup.

Then stop. Do not read another skill or run a script.
