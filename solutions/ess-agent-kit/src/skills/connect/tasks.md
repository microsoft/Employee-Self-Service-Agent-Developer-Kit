# Connect — Integration Templates

This folder does not use a top-level tasks.md. Each integration has its
own tasks.md and config.json under its subfolder:

- `servicenow/tasks.md` + `servicenow/config.json`
- `workday/tasks.md` + `workday/config.json` (future)

See SKILL.md for routing logic.
