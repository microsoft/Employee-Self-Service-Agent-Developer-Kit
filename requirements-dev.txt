# Test-only dependencies. Install with:
#   pip install -r requirements-dev.txt
#
# Mirrors [project.optional-dependencies].test in pyproject.toml so contributors
# who don't want to install the project as a package can still get the test deps.
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
vcrpy>=6.0,<8.0
responses>=0.25,<1.0
respx>=0.21,<1.0
PyYAML>=6.0,<7.0
